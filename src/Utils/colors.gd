class_name GameColors
extends RefCounted

# Colors for various game events and statuses
const PLAYER_ATTACK = Color("e0e0e0") # Color for player attack
const ENEMY_ATTACK = Color("ffc0c0") # Color for enemy attack
const STATUS_EFFECT_APPLIED = Color("3fff3f") # Color for status effect applied
const DESCEND = Color("9f3fff") # Color for descend action

const PLAYER_DIE = Color("ff3030") # Color for player death
const ENEMY_DIE = Color("ffa030") # Color for enemy death
const HURT = Color("ff0000") # Color for hurt action

const INVALID = Color("ffff00") # Color for invalid action
const IMPOSSIBLE = Color("808080") # Color for impossible action
const ERROR = Color("ff4040") # Color for error

const WELCOME_TEXT = Color("20a0ff") # Color for welcome text
const HEALTH_RECOVERED = Color("00ff00") # Color for health recovered
